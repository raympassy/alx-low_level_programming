#include "main.h"
#include <stdio.h>

/**
 * add  - function to add two numbers
 *@a: first number
 *@b: second number
 *Return: zero or number value
*/

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
